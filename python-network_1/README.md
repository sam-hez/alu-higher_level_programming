# Python - Network #1

This project explores fetching internet resources using Python's `urllib` and `requests` packages. It covers making GET and POST requests, handling response headers, managing HTTP error codes, and interacting with JSON APIs.

## Learning Objectives

At the end of this project, you should be able to explain:
- How to fetch internet resources with the Python package `urllib`
- How to decode `urllib` body response
- How to use the Python package `requests`
- How to make HTTP GET, POST, PUT, etc. requests
- How to fetch and manipulate JSON resources
- How to interact with external services and APIs

## Requirements

### Python Scripts
- Interpreted/compiled on Ubuntu 14.04 LTS using `python3` (version 3.4.3)
- All files end with a new line
- The first line of all files is exactly `#!/usr/bin/python3`
- Code uses the PEP 8 style (version 1.7)
- All files are executable
- All modules, classes, and functions are documented
- Use `get` to access dictionary values by key
- Scripts are not executed when imported (`if __name__ == "__main__":`)

## Tasks

0. **What's my status? #0**: Fetch status using `urllib`.
1. **Response header value #0**: Display `X-Request-Id` from header using `urllib`.
2. **POST an email #0**: Send POST request with email using `urllib`.
3. **Error code #0**: Manage `HTTPError` exceptions using `urllib`.
4. **What's my status? #1**: Fetch status using `requests`.
5. **Response header value #1**: Display `X-Request-Id` from header using `requests`.
6. **POST an email #1**: Send POST request with email using `requests`.
7. **Error code #1**: Manage HTTP status codes using `requests`.
8. **Search API**: Send a POST request with a letter and handle JSON response using `requests`.
9. **My GitHub!**: Use GitHub API with Basic Authentication to display user ID.
