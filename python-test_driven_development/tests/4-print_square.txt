The ``4-print_square`` module
=========================

Using ``print_square``
--------------------

Importing the function:
    >>> print_square = __import__('4-print_square').print_square

Basic behavior:
    >>> print_square(4)
    ####
    ####
    ####
    ####
    >>> print_square(1)
    #
    >>> print_square(0)

Type error (not an integer):
    >>> try:
    ...     print_square("4")
    ... except Exception as e:
    ...     print(e)
    size must be an integer
    >>> try:
    ...     print_square(1.5)
    ... except Exception as e:
    ...     print(e)
    size must be an integer
    >>> try:
    ...     print_square(None)
    ... except Exception as e:
    ...     print(e)
    size must be an integer

Value error (negative):
    >>> try:
    ...     print_square(-1)
    ... except Exception as e:
    ...     print(e)
    size must be >= 0

Special case: float and negative
    >>> try:
    ...     print_square(-1.0)
    ... except Exception as e:
    ...     print(e)
    size must be an integer
